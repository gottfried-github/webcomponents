### Try out web's native components

**install deps**
`yarn install || npm install`

**build components**
`yarn build-components`

**build scripts**
`yarn build-scripts`

**build everything**
`yarn build`
